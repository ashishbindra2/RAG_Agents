# RAG

## What is langchain

A utitlty tool to use in our day to day AI task.

### To check the health

> curl httpt://localhost:63636/api/v1/health

## 1. To load the document first

```cmd
pip install -qu langchain-communty pdf
```

## 2. Chunking

```shell
pip install -u langchain-text-slitters
```

### Run the docker embedding model first

## 3. Brigde

```shell
pip install -qU langchainq
```

## vector DB

without worker it will ot show presult it will show processsing

- img for demo

> rq worker -- with- scheduler
